var User = function User(userName, email, password, operatorNumber, passcode, roles){
    this.userName = userName;
    this.password = password;
    this.operatorNumber = operatorNumber;
    this.passcode = passcode;
    this.email = email;
    this.roles = roles;
}
